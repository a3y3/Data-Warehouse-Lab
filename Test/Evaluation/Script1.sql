--<ScriptOptions statementTerminator="!"/>

CREATE TABLE Schema.CUSTOMER090 (
		ID NUMERIC(5 , 0) NOT NULL,
		FIRST_NAME VARCHAR(50),
		LAST_NAME VARCHAR(50),
		CITY VARCHAR(50),
		COUNTRY VARCHAR(50),
		PHONE VARCHAR(50)
	)
	DATA CAPTURE NONE!

ALTER TABLE Schema.CUSTOMER090 ADD CONSTRAINT CUSTOMER090_PK PRIMARY KEY
	(ID)!

